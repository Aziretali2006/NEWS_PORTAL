export interface IHeaderList {
  title: string,
  id: number,
  to: string
};

export interface ILanguageList {
  value: string,
  label: string,
  img: string
}

export interface ICategoryList {
  id: number,
  title: string
}